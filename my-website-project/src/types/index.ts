export interface Product {
    id: number;
    name: string;
    price: number;
    image: string;
}

export interface User {
    id: number;
    username: string;
    email: string;
    password: string;
}